{
    "version": 2,
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/shopis/server/server.js"
        },
        {
            "src": "/(.*)",
            "dest": "/shopis/$1"
        }
    ]
}